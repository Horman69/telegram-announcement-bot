# 🚀 Деплой Telegram бота на VPS

Полная инструкция по развертыванию бота на VPS сервере (Hetzner, DigitalOcean, Vultr и др.)

## 📋 Содержание

- [Требования](#требования)
- [Шаг 1: Настройка VPS](#шаг-1-настройка-vps)
- [Шаг 2: Установка необходимого ПО](#шаг-2-установка-необходимого-по)
- [Шаг 3: Деплой бота](#шаг-3-деплой-бота)
- [Шаг 4: Настройка автозапуска](#шаг-4-настройка-автозапуска)
- [Управление ботом](#управление-ботом)
- [Мониторинг и логи](#мониторинг-и-логи)
- [Troubleshooting](#troubleshooting)

---

## Требования

### Минимальные характеристики VPS:
- **CPU**: 1 core
- **RAM**: 1GB (рекомендуется 2GB)
- **Storage**: 20GB SSD
- **OS**: Ubuntu 22.04 LTS (рекомендуется)

### Рекомендуемые провайдеры:
- **Hetzner** - €4.5/мес (лучшая цена/качество)
- **DigitalOcean** - $6/мес (простота)
- **Vultr** - $6/мес (много локаций)

---

## Шаг 1: Настройка VPS

### 1.1 Подключение к серверу

После создания VPS вы получите:
- IP адрес сервера
- Root пароль (или SSH ключ)

Подключитесь по SSH:

```bash
ssh root@YOUR_SERVER_IP
```

### 1.2 Обновление системы

```bash
# Обновляем список пакетов
apt update

# Обновляем установленные пакеты
apt upgrade -y
```

### 1.3 Создание пользователя для бота

**Важно:** Не запускайте бота от root!

```bash
# Создаем пользователя
adduser botuser

# Добавляем в группу sudo
usermod -aG sudo botuser

# Переключаемся на нового пользователя
su - botuser
```

### 1.4 Настройка firewall (опционально, но рекомендуется)

```bash
# Устанавливаем UFW
sudo apt install ufw -y

# Разрешаем SSH
sudo ufw allow 22

# Если будет сайт, разрешаем HTTP/HTTPS
sudo ufw allow 80
sudo ufw allow 443

# Включаем firewall
sudo ufw enable
```

---

## Шаг 2: Установка необходимого ПО

### 2.1 Установка Node.js

```bash
# Устанавливаем Node.js 20.x (LTS)
curl -fsSL https://deb.nodesource.com/setup_20.x | sudo -E bash -
sudo apt install -y nodejs

# Проверяем установку
node --version  # должно показать v20.x.x
npm --version   # должно показать 10.x.x
```

### 2.2 Установка PM2

PM2 - менеджер процессов для Node.js приложений.

```bash
# Устанавливаем PM2 глобально
sudo npm install -g pm2

# Проверяем установку
pm2 --version
```

### 2.3 Установка Git

```bash
# Устанавливаем Git
sudo apt install git -y

# Проверяем установку
git --version
```

---

## Шаг 3: Деплой бота

### 3.1 Клонирование репозитория

```bash
# Переходим в домашнюю директорию
cd ~

# Клонируем репозиторий (замените на ваш URL)
git clone https://github.com/YOUR_USERNAME/telegram-announcement-bot.git

# Переходим в директорию проекта
cd telegram-announcement-bot
```

### 3.2 Установка зависимостей

```bash
# Устанавливаем npm пакеты
npm install --production
```

### 3.3 Настройка переменных окружения

```bash
# Создаем .env файл
nano .env
```

Вставьте следующее содержимое:

```env
# Telegram Bot Token (получите от @BotFather)
TELEGRAM_BOT_TOKEN=YOUR_BOT_TOKEN_HERE

# Node Environment
NODE_ENV=production
```

**Сохранение в nano:**
- Нажмите `Ctrl + O` (сохранить)
- Нажмите `Enter` (подтвердить имя файла)
- Нажмите `Ctrl + X` (выйти)

### 3.4 Создание директории для логов

```bash
# Создаем директорию для логов
mkdir -p logs
```

### 3.5 Запуск бота через PM2

```bash
# Запускаем бота
npm run deploy

# Или напрямую через PM2
pm2 start ecosystem.config.js --env production
```

### 3.6 Проверка статуса

```bash
# Проверяем статус бота
pm2 status

# Должно показать:
# ┌─────┬──────────────────┬─────────┬─────────┬──────────┐
# │ id  │ name             │ status  │ restart │ uptime   │
# ├─────┼──────────────────┼─────────┼─────────┼──────────┤
# │ 0   │ announcement-bot │ online  │ 0       │ 2s       │
# └─────┴──────────────────┴─────────┴─────────┴──────────┘
```

---

## Шаг 4: Настройка автозапуска

Настроим PM2 для автоматического запуска при перезагрузке сервера.

```bash
# Сохраняем текущий список процессов PM2
pm2 save

# Настраиваем автозапуск PM2 при старте системы
pm2 startup

# PM2 покажет команду, которую нужно выполнить
# Скопируйте и выполните её (будет что-то вроде):
# sudo env PATH=$PATH:/usr/bin /usr/lib/node_modules/pm2/bin/pm2 startup systemd -u botuser --hp /home/botuser
```

**Проверка автозапуска:**

```bash
# Перезагружаем сервер
sudo reboot

# После перезагрузки подключаемся снова
ssh botuser@YOUR_SERVER_IP

# Проверяем, что бот запустился автоматически
pm2 status
```

---

## Управление ботом

### Основные команды PM2:

```bash
# Просмотр статуса
npm run status
# или
pm2 status

# Просмотр логов (в реальном времени)
npm run logs
# или
pm2 logs announcement-bot

# Перезапуск бота
npm run restart
# или
pm2 restart announcement-bot

# Остановка бота
npm run stop
# или
pm2 stop announcement-bot

# Удаление бота из PM2
pm2 delete announcement-bot

# Просмотр детальной информации
pm2 show announcement-bot
```

### Обновление бота:

```bash
# Переходим в директорию проекта
cd ~/telegram-announcement-bot

# Останавливаем бота
pm2 stop announcement-bot

# Получаем последние изменения из Git
git pull origin main

# Устанавливаем новые зависимости (если есть)
npm install --production

# Перезапускаем бота
pm2 restart announcement-bot

# Проверяем логи
pm2 logs announcement-bot --lines 50
```

---

## Мониторинг и логи

### Просмотр логов:

```bash
# Логи в реальном времени
pm2 logs announcement-bot

# Последние 100 строк логов
pm2 logs announcement-bot --lines 100

# Только ошибки
pm2 logs announcement-bot --err

# Очистить логи
pm2 flush
```

### Файлы логов:

Логи сохраняются в директории `logs/`:
- `logs/error.log` - ошибки
- `logs/out.log` - обычный вывод

```bash
# Просмотр логов через tail
tail -f logs/out.log
tail -f logs/error.log

# Просмотр последних 50 строк
tail -n 50 logs/error.log
```

### Мониторинг ресурсов:

```bash
# Мониторинг CPU и памяти
pm2 monit

# Детальная информация о процессе
pm2 show announcement-bot
```

---

## Запуск нескольких ботов

Если у вас несколько ботов, создайте отдельные директории:

```bash
cd ~
mkdir bots
cd bots

# Клонируем разные боты
git clone https://github.com/user/bot1.git
git clone https://github.com/user/bot2.git
git clone https://github.com/user/bot3.git

# Для каждого бота:
cd bot1
npm install --production
nano .env  # настраиваем переменные
pm2 start ecosystem.config.js --env production

cd ../bot2
npm install --production
nano .env
pm2 start ecosystem.config.js --env production

# И так далее...
```

**Просмотр всех ботов:**

```bash
pm2 status

# Должно показать:
# ┌─────┬──────────────────┬─────────┬─────────┬──────────┐
# │ id  │ name             │ status  │ restart │ uptime   │
# ├─────┼──────────────────┼─────────┼─────────┼──────────┤
# │ 0   │ announcement-bot │ online  │ 0       │ 2h       │
# │ 1   │ birthday-bot     │ online  │ 0       │ 1h       │
# │ 2   │ support-bot      │ online  │ 0       │ 30m      │
# └─────┴──────────────────┴─────────┴─────────┴──────────┘
```

---

## Troubleshooting

### Бот не запускается

```bash
# Проверяем логи ошибок
pm2 logs announcement-bot --err

# Проверяем, что .env файл существует
ls -la .env

# Проверяем содержимое .env
cat .env

# Проверяем, что токен правильный
# Отправьте /start вашему боту в Telegram
```

### Бот падает и перезапускается

```bash
# Смотрим логи
pm2 logs announcement-bot --lines 100

# Проверяем использование памяти
pm2 monit

# Если превышает лимит, увеличьте в ecosystem.config.js:
# max_memory_restart: '500M'
```

### Бот не отвечает на команды

```bash
# Проверяем, что бот запущен
pm2 status

# Проверяем логи
pm2 logs announcement-bot

# Перезапускаем бота
pm2 restart announcement-bot
```

### Проблемы с правами доступа

```bash
# Проверяем владельца файлов
ls -la

# Если нужно, меняем владельца
sudo chown -R botuser:botuser ~/telegram-announcement-bot
```

### Нет места на диске

```bash
# Проверяем использование диска
df -h

# Очищаем логи PM2
pm2 flush

# Очищаем старые логи
rm -f logs/*.log

# Очищаем кэш npm
npm cache clean --force
```

---

## Бэкап данных

### Создание бэкапа:

```bash
# Создаем директорию для бэкапов
mkdir -p ~/backups

# Создаем бэкап данных бота
tar -czf ~/backups/bot-backup-$(date +%Y%m%d).tar.gz \
  ~/telegram-announcement-bot/data

# Список бэкапов
ls -lh ~/backups
```

### Автоматический бэкап (cron):

```bash
# Открываем crontab
crontab -e

# Добавляем задачу (бэкап каждый день в 3:00 ночи)
0 3 * * * tar -czf ~/backups/bot-backup-$(date +\%Y\%m\%d).tar.gz ~/telegram-announcement-bot/data

# Удаление старых бэкапов (старше 7 дней)
0 4 * * * find ~/backups -name "bot-backup-*.tar.gz" -mtime +7 -delete
```

---

## Полезные ссылки

- [PM2 Documentation](https://pm2.keymetrics.io/docs/usage/quick-start/)
- [Node.js Best Practices](https://github.com/goldbergyoni/nodebestpractices)
- [Telegraf Documentation](https://telegraf.js.org/)
- [Ubuntu Server Guide](https://ubuntu.com/server/docs)

---

## Поддержка

Если возникли проблемы:
1. Проверьте логи: `pm2 logs announcement-bot`
2. Проверьте статус: `pm2 status`
3. Перезапустите бота: `pm2 restart announcement-bot`

**Создано с использованием Antigravity AI** 🚀
