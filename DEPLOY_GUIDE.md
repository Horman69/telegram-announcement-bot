# 🚀 Инструкция по деплою на сервер Вексель

## Предварительные требования

- Node.js 16+ установлен на сервере
- Доступ по SSH
- Git установлен
- PM2 установлен глобально (`npm install -g pm2`)

## Шаг 1: Клонировать репозиторий

```bash
cd /opt/bots
git clone https://github.com/YOUR_USERNAME/telegram-bot.git
cd telegram-bot
```

## Шаг 2: Установить зависимости

```bash
npm install
```

## Шаг 3: Создать файл .env

```bash
cat > .env << EOF
TELEGRAM_BOT_TOKEN=YOUR_BOT_TOKEN_HERE
EOF
```

## Шаг 4: Проверить работоспособность

```bash
npm start
```

После запуска проверить в Telegram:
- `/start` - должно открыться меню
- `/menu` - проверить все кнопки
- `/addadmin` (для админа) - проверить добавление администратора

## Шаг 5: Запустить в фоне с PM2

```bash
pm2 start src/bot.js --name "telegram-bot"
pm2 save
pm2 startup
```

## Шаг 6: Мониторинг

```bash
# Посмотреть логи
pm2 logs telegram-bot

# Посмотреть статус
pm2 status

# Перезапустить
pm2 restart telegram-bot

# Остановить
pm2 stop telegram-bot
```

## Обновление кода

```bash
cd /opt/bots/telegram-bot
git pull
npm install  # если добавлены новые зависимости
pm2 restart telegram-bot
```

## Решение проблем

### Бот не отвечает
```bash
# Проверить логи
pm2 logs telegram-bot

# Перезапустить
pm2 restart telegram-bot
```

### Ошибка токена
```bash
# Проверить токен в .env
cat .env

# Обновить если нужно
echo "TELEGRAM_BOT_TOKEN=NEW_TOKEN" > .env
pm2 restart telegram-bot
```

### Ошибка портов
```bash
# Найти процесс на порту
netstat -tuln | grep :3000

# Убить процесс если нужно
kill -9 PID
```

---

**Контакты поддержки:** admin@example.com  
**Версия бота:** 2.0  
**Дата:** 5 декабря 2025
