# 🚀 Полное руководство по функциям бота

## 📋 Содержание
- [Базовые команды](#-базовые-команды)
- [Рассылка объявлений](#-рассылка-объявлений)
- [Система шаблонов](#-система-шаблонов)
- [Теги и категории](#-теги-и-категории)
- [Управление группами](#-управление-группами)
- [Как стать администратором](#-как-стать-администратором)

---

## 🎯 Базовые команды

### Для всех пользователей:

| Команда | Описание |
|---------|----------|
| `/start` | Приветственное сообщение и краткая информация о боте |
| `/help` | Полная справка по всем командам с примерами |
| `/myid` | Узнать свой Telegram ID (работает в группах и личных сообщениях) |

---

## 📢 Рассылка объявлений

### 1. Базовая рассылка во все группы

**Команда:** `/announce <текст>`

**Пример:**
```
/announce Важное объявление для всех участников!
```

**Как работает:**
1. Отправляете команду с текстом
2. Бот показывает превью объявления
3. Подтверждаете отправку кнопкой "✅ Да, отправить"
4. Объявление отправляется во все зарегистрированные группы

---

### 2. Рассылка с медиа-файлами 📎

**Команда:** `/announce_media`

**Поддерживаемые типы файлов:**
- 📷 Фото
- 🎥 Видео
- 📄 Документы (PDF, Word, Excel и др.)
- 🎵 Аудио

**Как использовать:**

1. Отправьте команду `/announce_media`
2. Отправьте медиа-файл (фото, видео, документ или аудио)
3. Отправьте текст (подпись) для объявления
4. Проверьте превью и подтвердите отправку

**Пример:**
```
1. /announce_media
2. [Отправляете фото]
3. Новый продукт уже в продаже! 🎉
4. [Нажимаете "✅ Да, отправить"]
```

---

### 3. Выборочная рассылка по тегам 🏷️

**Команда:** `/announce_to <теги> <текст>`

**Пример:**
```
/announce_to новости,важное Срочная новость для всех!
```

**Как работает:**
- Объявление отправляется только в группы с указанными тегами
- Можно указать несколько тегов через запятую
- Группа должна иметь хотя бы один из указанных тегов

**Пример с одним тегом:**
```
/announce_to новости Сегодняшние новости
```

---

### 4. Рассылка в конкретные группы по ID

**Команда:** `/announce_groups <id1,id2> <текст>`

**Пример:**
```
/announce_groups -1001601437600 Сообщение для конкретной группы
```

**Для нескольких групп:**
```
/announce_groups -1001601437600,-1001234567890 Сообщение для двух групп
```

**Как узнать ID группы:**
Используйте команду `/groups` - она покажет все группы с их ID.

---

## 📋 Система шаблонов

Шаблоны позволяют сохранять часто используемые тексты и быстро отправлять их.

### Создание шаблона

**Команда:** `/template_save <название>`

**Пример:**
```
1. /template_save приветствие
2. [Отправляете текст:]
   Добро пожаловать в нашу группу! 👋
   Мы рады видеть вас здесь.
```

**Правила:**
- Название шаблона должно быть одним словом (или через_подчеркивание)
- Текст шаблона отправляется отдельным сообщением после команды

---

### Просмотр всех шаблонов

**Команда:** `/template_list`

**Что показывает:**
- Список всех сохраненных шаблонов
- Интерактивные кнопки для просмотра каждого шаблона
- Возможность использовать или удалить шаблон прямо из списка

---

### Использование шаблона

**Команда:** `/template_use <название>`

**Пример:**
```
/template_use приветствие
```

**Как работает:**
1. Бот показывает превью шаблона
2. Вы подтверждаете отправку
3. Шаблон отправляется во все группы

---

### Удаление шаблона

**Команда:** `/template_delete <название>`

**Пример:**
```
/template_delete приветствие
```

**Важно:** Удаление нельзя отменить!

---

## 🏷️ Теги и категории

Теги позволяют категоризировать группы для удобной выборочной рассылки.

### Добавление тега группе

**Команда:** `/tag_add <group_id> <тег>`

**Пример:**
```
/tag_add -1001601437600 новости
```

**Можно добавить несколько тегов:**
```
/tag_add -1001601437600 новости
/tag_add -1001601437600 важное
/tag_add -1001601437600 срочное
```

---

### Удаление тега у группы

**Команда:** `/tag_remove <group_id> <тег>`

**Пример:**
```
/tag_remove -1001601437600 новости
```

---

### Просмотр всех тегов

**Команда:** `/tag_list`

**Что показывает:**
- Список всех доступных тегов
- Количество групп для каждого тега
- Подсказку по использованию тегов для рассылки

**Пример вывода:**
```
📋 Доступные теги (3):

1. #новости (5 групп)
2. #важное (3 группы)
3. #срочное (2 группы)

💡 Используйте /announce_to <теги> <текст> для рассылки по тегам.
```

---

## 📊 Управление группами

### Просмотр всех групп

**Команда:** `/groups`

**Что показывает:**
- Название группы
- ID группы (для использования в командах)
- Теги группы
- Дата добавления бота в группу

**Пример вывода:**
```
📊 Зарегистрированные группы (1):

1. pro_bot
   ID: -1001601437600
   Теги: #новости
   Добавлена: 27.11.2025, 15:06
```

---

### Автоматическое добавление групп

**Группы добавляются автоматически!** ✅

Просто добавьте бота в группу - он автоматически:
- Сохранит ID группы
- Сохранит название группы
- Отправит приветственное сообщение
- Добавит группу в список для рассылки

**Удаление группы:**
Если вы удалите бота из группы, она автоматически удалится из списка.

---

## 👤 Как стать администратором

### Шаг 1: Узнать свой Telegram ID

Отправьте боту команду:
```
/myid
```

Бот ответит вашим ID, например: `221615370`

---

### Шаг 2: Добавить ID в конфигурацию

Откройте файл `src/config/admins.js` и добавьте свой ID:

```javascript
export const ADMIN_IDS = [
    221615370,  // Руслан (@norman_p3)
    123456789   // Ваш ID
];
```

---

### Шаг 3: Перезапустить бота

В терминале нажмите `Ctrl+C`, затем:
```bash
npm start
```

**Готово!** Теперь у вас есть доступ ко всем командам администратора.

---

## 💡 Полезные советы

### 🎯 Организация групп с тегами

**Рекомендуемые теги:**
- `новости` - для новостных объявлений
- `важное` - для важных сообщений
- `срочное` - для срочных уведомлений
- `общее` - для общих объявлений
- `события` - для анонсов событий

**Пример использования:**
```
/tag_add -1001601437600 новости
/tag_add -1001601437600 важное

/announce_to новости Сегодняшние новости
/announce_to важное,срочное Срочное объявление!
```

---

### 📋 Эффективное использование шаблонов

**Создайте шаблоны для:**
- Приветствия новых участников
- Еженедельных объявлений
- Правил группы
- Часто задаваемых вопросов
- Рекламных сообщений

**Пример:**
```
/template_save правила
Правила нашей группы:
1. Будьте вежливы
2. Не спамьте
3. Помогайте другим

/template_save еженедельный_отчет
📊 Еженедельный отчет:
[Здесь ваш шаблон отчета]
```

---

### 🔍 Поиск ID группы

Если вы не помните ID группы:

1. Используйте `/groups` - покажет все группы с ID
2. ID группы всегда начинается с `-100`
3. Скопируйте ID для использования в командах

---

## ⚠️ Важные замечания

### Права администратора

- ❌ Администраторы НЕ добавляются автоматически
- ✅ Нужно вручную добавить ID в `src/config/admins.js`
- ✅ После добавления нужно перезапустить бота

### Группы

- ✅ Группы добавляются автоматически при добавлении бота
- ✅ Группы удаляются автоматически при удалении бота
- ✅ Бот должен иметь права на отправку сообщений в группе

### Безопасность

- 🔐 Токен бота хранится в `.env` (не попадает в git)
- 🔐 Только администраторы могут создавать объявления
- 🔐 Все действия логируются
- 🔐 **Команды в группах**: Большинство команд работают только через бота в личных сообщениях
  - ✅ Разрешены в группах: `/myid`, `/groupid` (для получения ID)
  - ❌ Заблокированы в группах: все административные команды

---

## 📞 Поддержка

Если у вас возникли проблемы:

1. Проверьте, что бот запущен (`npm start`)
2. Проверьте, что ваш ID добавлен в `src/config/admins.js`
3. Проверьте логи бота в консоли
4. Используйте `/help` для просмотра всех команд

---

**Создано с использованием Antigravity AI** 🚀
