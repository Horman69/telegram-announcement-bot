# 📢 Telegram Announcement Bot

Telegram бот для рассылки объявлений в группы с модульной архитектурой и системой администрирования.

## ✨ Возможности

### Базовые функции:
- 📢 **Рассылка объявлений** в несколько групп одновременно
- 🔐 **Система администрирования** - только авторизованные пользователи могут создавать объявления
- ✅ **Подтверждение отправки** через inline-кнопки
- 📝 **Автоматическое сохранение групп** - бот запоминает группы, куда его добавляют
- 📊 **Детальное логирование** всех действий и ошибок
- 🎯 **Модульная архитектура** - чистый и расширяемый код

### Расширенные функции:
- 📎 **Рассылка с медиа-файлами** - отправка объявлений с фото, видео, документами и аудио
- 📋 **Система шаблонов** - сохранение и повторное использование часто используемых текстов
- 🏷️ **Теги групп** - категоризация групп для удобного управления
- 🎯 **Выборочная рассылка** - отправка объявлений только в определенные группы по тегам или ID

## 📁 Структура проекта

```
Test/
├── src/
│   ├── commands/              # Обработчики команд
│   │   ├── announce.js       # Команда /announce (базовая рассылка)
│   │   ├── announceMedia.js  # Команда /announce_media (рассылка с медиа)
│   │   ├── announceSelective.js # Команды /announce_to и /announce_groups
│   │   ├── template.js       # Команды для работы с шаблонами
│   │   ├── tags.js           # Команды для работы с тегами
│   │   ├── groups.js         # Команда /groups
│   │   └── myid.js           # Команда /myid
│   ├── services/             # Сервисы
│   │   ├── logger.js         # Логирование
│   │   ├── groupManager.js   # Управление группами и тегами
│   │   ├── templateManager.js # Управление шаблонами
│   │   └── conversationState.js # Управление состояниями диалогов
│   ├── config/               # Конфигурация
│   │   └── admins.js         # Список администраторов
│   └── bot.js               # Главный файл бота
├── data/
│   ├── groups.json          # Хранилище групп и тегов
│   └── templates.json       # Хранилище шаблонов
├── .env                     # Переменные окружения (токен)
├── .env.example             # Пример .env файла
├── package.json             # Зависимости проекта
└── README.md               # Документация
```

## 🚀 Быстрый старт

### 1️⃣ Установка зависимостей

```bash
npm install
```

### 2️⃣ Настройка токена

Токен бота уже настроен в файле `.env`. Если нужно изменить:

1. Откройте `.env`
2. Измените значение `TELEGRAM_BOT_TOKEN`

### 3️⃣ Получение своего Telegram ID

Запустите бота:

```bash
npm start
```

Откройте бота в Telegram и отправьте команду:

```
/myid
```

Скопируйте полученный ID (например: `123456789`)

### 4️⃣ Добавление себя в администраторы

1. Откройте файл `src/config/admins.js`
2. Добавьте свой ID в массив `ADMIN_IDS`:

```javascript
export const ADMIN_IDS = [
  123456789  // Ваш ID
];
```

3. Сохраните файл и перезапустите бота (Ctrl+C, затем `npm start`)

### 5️⃣ Создание тестовой группы

1. Создайте группу в Telegram (минимум 2 участника)
2. Добавьте бота в группу
3. Бот автоматически сохранит ID группы в `data/groups.json`

### 6️⃣ Тестирование рассылки

Отправьте боту в личные сообщения:

```
/announce Тестовое объявление!
```

Нажмите кнопку "✅ Да, отправить" - объявление будет отправлено во все группы.

## 📝 Команды бота

### Для всех пользователей:

- `/start` - Приветственное сообщение
- `/help` - Полная справка по всем командам
- `/myid` - Узнать свой Telegram ID (работает в группах и личных сообщениях)
- `/register` - Регистрация для получения рассылок от бота

### Для администраторов:

#### 👤 Управление пользователями:

- `/users` - Просмотр и управление зарегистрированными пользователями
- `/announce_users <текст>` - Рассылка сообщения всем одобренным пользователям
- `/announce_subject <предмет> <текст>` - Рассылка пользователям с определённым предметом

#### 📢 Рассылка объявлений:

- `/announce <текст>` - Рассылка во все зарегистрированные группы
- `/announce_media` - Рассылка с медиа-файлом (фото, видео, документ, аудио)
- `/announce_to <теги> <текст>` - Рассылка только в группы с указанными тегами
- `/announce_groups <id1,id2> <текст>` - Рассылка в конкретные группы по ID

#### 📋 Управление шаблонами:

- `/template_save <название>` - Сохранить новый шаблон
- `/template_list` - Показать все сохраненные шаблоны
- `/template_use <название>` - Использовать шаблон для рассылки
- `/template_delete <название>` - Удалить шаблон

#### 🏷️ Управление тегами:

- `/tag_add <group_id> <тег>` - Добавить тег группе
- `/tag_remove <group_id> <тег>` - Удалить тег у группы
- `/tag_list` - Показать все доступные теги

#### 📊 Управление группами:

- `/groups` - Посмотреть список зарегистрированных групп с кнопками удаления
- `/groupid` - Узнать ID текущей группы (работает в группах и личных сообщениях)
- `/addgroup` - Добавить группу вручную по ID
- `/removegroup` - Удалить группу из списка рассылки

#### 👥 Управление администраторами:

- `/addadmin` - Добавить нового администратора
- `/removeadmin` - Удалить администратора

## 💡 Примеры использования

### 1. Базовая рассылка

```
/announce Важное объявление для всех участников!
```

Бот покажет превью и кнопки подтверждения. После подтверждения объявление будет отправлено во все группы.

### 2. Рассылка с медиа-файлом

```
/announce_media
```

Затем:
1. Отправьте фото, видео, документ или аудио
2. Отправьте текст (подпись) для объявления
3. Подтвердите отправку

### 3. Работа с шаблонами

**Создание шаблона:**
```
/template_save приветствие
```
Затем отправьте текст:
```
Добро пожаловать в нашу группу! 👋
Мы рады видеть вас здесь.
```

**Использование шаблона:**
```
/template_use приветствие
```

**Просмотр всех шаблонов:**
```
/template_list
```

### 4. Работа с тегами

**Добавление тега группе:**
```
/tag_add -1001601437600 новости
```

**Просмотр всех тегов:**
```
/tag_list
```

**Рассылка по тегам:**
```
/announce_to новости,важное Срочная новость для всех!
```

Объявление будет отправлено только в группы с тегами "новости" или "важное".

### 5. Рассылка в конкретные группы

```
/announce_groups -1001601437600,-1001234567890 Сообщение для двух групп
```

### 6. Управление группами

**Получить ID группы:**
```
# Отправьте в группе:
/groupid
```

Бот ответит с ID и названием группы.

**Добавить группу вручную:**
```
/addgroup
```

Затем:
1. Отправьте ID группы (например: `-1001234567890`)
2. Отправьте название группы
3. Подтвердите добавление

**Удалить группу из списка:**
```
/removegroup
```

Или используйте кнопки удаления в команде `/groups`.

### 7. Просмотр групп

```
/groups
```

Показывает список всех групп с их ID, названиями, тегами и датой добавления.

## 🔧 Скрипты

```bash
# Обычный запуск
npm start

# Запуск с автоперезагрузкой при изменении файлов (для разработки)
npm run dev
```

## 📊 Логирование

Бот логирует все важные события:

- `[INFO]` - Информационные сообщения
- `[SUCCESS]` - Успешные операции
- `[WARN]` - Предупреждения
- `[ERROR]` - Ошибки

Пример лога:
```
[INFO] 2025-11-27T14:48:49.123Z: Bot started successfully!
[SUCCESS] 2025-11-27T14:48:50.456Z: Added group: Тестовая группа (-1001234567890)
[INFO] 2025-11-27T14:49:00.789Z: Admin 123456789 created announcement draft
[SUCCESS] 2025-11-27T14:49:05.012Z: Announcement sent to group Тестовая группа (-1001234567890)
```

## 🛡️ Безопасность

- ✅ Токен бота хранится в `.env` (не попадает в git)
- ✅ Только администраторы могут создавать объявления
- ✅ Проверка прав на каждую команду
- ✅ Логирование всех действий
- ✅ **Команды в группах**: Большинство команд работают только в личных сообщениях с ботом
  - Разрешены в группах: `/myid`, `/groupid` (для получения ID)
  - Заблокированы в группах: все административные команды (рассылка, управление и т.д.)

## ❓ Troubleshooting

### Бот не отвечает

1. Проверьте, что бот запущен (`npm start`)
2. Проверьте токен в `.env`
3. Убедитесь, что бот не заблокирован в Telegram

### Команда /announce не работает

1. Проверьте, что ваш ID добавлен в `src/config/admins.js`
2. Перезапустите бота после изменения конфигурации
3. Используйте `/myid` для проверки своего ID

### Объявление не приходит в группу

1. Убедитесь, что бот добавлен в группу
2. Проверьте список групп командой `/groups`
3. Проверьте логи на наличие ошибок

### Группа не сохраняется автоматически

1. Убедитесь, что бот был добавлен ПОСЛЕ его запуска
2. Проверьте права бота в группе
3. Проверьте файл `data/groups.json`

## 🔄 Добавление новых команд

Пример добавления команды `/time`:

1. Создайте файл `src/commands/time.js`:

```javascript
import logger from '../services/logger.js';

export function setupTimeCommand(bot) {
  bot.command('time', (ctx) => {
    const time = new Date().toLocaleTimeString('ru-RU');
    ctx.reply(`🕐 Текущее время: ${time}`);
    logger.info(`User ${ctx.from.id} requested time`);
  });
}
```

2. Импортируйте и зарегистрируйте в `src/bot.js`:

```javascript
import { setupTimeCommand } from './commands/time.js';

// В функции initializeCommands():
setupTimeCommand(bot);
```

## 📚 Технологии

- **Node.js** - JavaScript runtime
- **Telegraf v4** - Библиотека для Telegram Bot API
- **dotenv** - Управление переменными окружения
- **ES Modules** - Современный синтаксис импортов

## 📄 Лицензия

ISC

