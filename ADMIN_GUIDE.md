# 📖 Руководство администратора - Telegram Announcement Bot

## 🎯 Для чего нужен этот бот?

Бот предназначен для **массовой рассылки объявлений** в несколько Telegram-групп одновременно. Вместо того, чтобы вручную копировать и отправлять одно и то же сообщение в каждую группу, вы отправляете одну команду боту, и он автоматически рассылает сообщение во все зарегистрированные группы.

---

## 📋 Основные возможности

### 1. 📣 Базовая рассылка

**Команда:** `/announce <текст>`

**Для чего:** Отправить одно и то же сообщение во все зарегистрированные группы.

**Пример:**
```
/announce Важное объявление для всех сотрудников!
```

**Что происходит:**
- Бот отправляет сообщение во все группы из списка
- Показывает статистику: сколько отправлено успешно, сколько ошибок
- Логирует все действия для отчётности

**Особенности:**
- Поддерживает HTML-форматирование (`<b>жирный</b>`, `<i>курсив</i>`)
- Автоматически экранирует спецсимволы
- Отправляет в установленные темы форумов (если настроено)

---

### 2. 🏷️ Рассылка по тегам

**Команда:** `/announce_to <теги> <текст>`

**Для чего:** Отправить сообщение только в группы с определёнными тегами.

**Пример:**
```
/announce_to важное,новости Срочное объявление!
```

**Что происходит:**
- Бот находит все группы с тегами "важное" ИЛИ "новости"
- Отправляет сообщение только в эти группы
- Показывает список групп, в которые отправлено

**Как работают теги:**
1. Добавьте тег к группе: `/tag_add важное` (выполните в группе)
2. Просмотрите все теги: `/tag_list`
3. Удалите тег: `/tag_remove важное`

**Особенности:**
- Теги указываются через запятую
- Регистр не важен (важное = ВАЖНОЕ)
- Одна группа может иметь несколько тегов

---

### 3. 🎯 Рассылка по ID групп

**Команда:** `/announce_groups <id1,id2> <текст>`

**Для чего:** Отправить сообщение в конкретные группы по их ID.

**Пример:**
```
/announce_groups -1001838199188,-1002226753104 Сообщение для двух групп
```

**Как узнать ID группы:**
1. Зайдите в группу
2. Отправьте команду `/groupid`
3. Бот покажет ID группы

**Особенности:**
- ID всегда начинается с `-100`
- ID указываются через запятую без пробелов
- Можно отправить в одну группу или в несколько

---

### 4. 📎 Рассылка с медиа

**Команда:** `/announce_media`

**Для чего:** Отправить рассылку с фото, видео, документом или аудио.

**Как использовать:**
1. Отправьте команду `/announce_media`
2. Отправьте медиа-файл (фото, видео, документ или аудио)
3. Добавьте подпись к файлу (необязательно)
4. Бот отправит во все группы

**Пример:**
```
1. /announce_media
2. [отправляете фото]
3. Бот: "Отправить это фото во все группы?"
4. Нажимаете "✅ Да"
```

**Особенности:**
- Поддерживает: фото, видео, документы, аудио
- Подпись поддерживает HTML-форматирование
- Можно отправить с подписью или без

---

### 5. 📎🏷️ Рассылка медиа по тегам

**Команда:** `/mediatags <теги>`

**Для чего:** Отправить медиа только в группы с определёнными тегами.

**Как использовать:**
1. Отправьте команду `/mediatags важное,новости`
2. Отправьте медиа-файл с подписью
3. Бот отправит только в группы с этими тегами

**Особенности:**
- Комбинирует возможности `/announce_to` и `/announce_media`
- Удобно для целевых рассылок с изображениями

---

## 💬 Работа с форумами Telegram

### Что это такое?

Если ваша группа - **форум** (имеет несколько тем), вы можете настроить бота так, чтобы рассылки шли не в General, а в конкретную тему.

### Зачем это нужно?

**Пример:** У вас есть форум "Компания" с темами:
- 📢 Объявления
- 💼 Работа
- 🎉 События

Вы хотите, чтобы рассылки шли только в тему "📢 Объявления", а не в General.

### Как настроить?

#### Шаг 1: Найти ID темы

1. Откройте нужную тему в Telegram Desktop или Web
2. Посмотрите на URL в адресной строке:
   ```
   https://t.me/c/1838199188/1
   ```
3. Число после последнего `/` - это ID темы (в примере: `1`)

#### Шаг 2: Установить тему

1. Зайдите в группу-форум
2. Отправьте команду:
   ```
   /settopic 1
   ```
3. Бот подтвердит установку

#### Шаг 3: Проверить

Отправьте `/groups` - группа будет отмечена иконкой 💬

### Как сбросить тему?

Если хотите вернуть рассылку в General:

```
/settopic reset
```

### Важные особенности:

✅ **Автоматическая работа:** После установки темы ВСЕ команды рассылки (`/announce`, `/announce_to`, `/announce_media`) автоматически отправляют в эту тему

✅ **Одна тема на группу:** Можно установить только одну тему для рассылки

✅ **Видимость:** В списке групп (`/groups`) группы с темой отмечены иконкой 💬

❌ **Нельзя выбрать несколько тем:** Бот не поддерживает отправку в разные темы одной группы одновременно

---

## 👥 Управление группами

### Автоматическое добавление

Когда вы добавляете бота в группу, она автоматически регистрируется в системе.

### Ручное добавление

**Команда:** `/addgroup`

**Когда использовать:**
- Бот не может отправлять сообщения в группу (нет прав)
- Группа была удалена и нужно добавить заново
- Хотите добавить группу до того, как бот получит права

**Как использовать:**
1. Отправьте `/addgroup`
2. Введите ID группы (например: `-1001234567890`)
3. Введите название группы
4. Подтвердите добавление

**⚠️ Важно:** ID группы ВСЕГДА начинается с `-100`. Если ввести неправильный формат, бот покажет ошибку.

### Удаление группы

**Команда:** `/groups` → кнопка "🗑️ Удалить"

**Когда использовать:**
- Группа больше не нужна в рассылке
- Бот был удалён из группы
- Группа была расформирована

---

## 🏷️ Управление тегами

### Добавить тег

**Команда:** `/tag_add <тег>` (выполнить в группе)

**Пример:**
```
/tag_add важное
```

### Удалить тег

**Команда:** `/tag_remove <тег>` (выполнить в группе)

### Посмотреть все теги

**Команда:** `/tag_list`

Показывает все теги и группы, которые их используют.

---

## 📝 Шаблоны сообщений

### Сохранить шаблон

**Команда:** `/template_save <название>`

**Для чего:** Сохранить часто используемый текст для быстрой рассылки.

**Пример:**
```
/template_save еженедельный_отчёт
```

Затем отправьте текст шаблона.

### Использовать шаблон

**Команда:** `/template_use <название>`

**Пример:**
```
/template_use еженедельный_отчёт
```

Бот отправит сохранённый текст во все группы.

### Посмотреть все шаблоны

**Команда:** `/template_list`

### Удалить шаблон

**Команда:** `/template_delete <название>`

---

## 👨‍💼 Управление администраторами

### Добавить администратора

**Команда:** `/addadmin`

Через меню или отправьте ID пользователя.

### Удалить администратора

**Команда:** `/removeadmin`

### Посмотреть список

**Команда:** `/listadmins`

---

## 👥 Управление пользователями

### Что это такое?

Система регистрации позволяет пользователям (например, учителям) зарегистрироваться для получения рассылок в личные сообщения. После одобрения администратором они будут получать объявления напрямую от бота.

### Регистрация пользователя

**Команда:** `/register`

**Процесс:**
1. Пользователь отправляет `/register`
2. Вводит фамилию, имя, отчество
3. Указывает предмет (например: "Математика")
4. Подтверждает данные
5. Заявка отправляется администратору

**Особенности:**
- Один пользователь = одна регистрация (защита от дубликатов)
- Можно отменить через `/cancel` на любом шаге
- Валидация длины полей (1-50 символов для ФИО, 1-100 для предмета)

### Одобрение/отклонение заявок

Когда пользователь регистрируется, все администраторы получают уведомление с кнопками:
- ✅ **Одобрить** - пользователь получит доступ к рассылкам
- ❌ **Отклонить** - заявка будет отклонена

**Через меню:**
1. Администратор → Пользователи → Ожидают одобрения
2. Используйте `/users` для управления

### Просмотр пользователей

**Команда:** `/users`

**Что показывает:**
- Статистика (всего, ожидают, одобрены, отклонены)
- Фильтры по статусу
- Возможность удаления пользователей

**Через меню:**
- Администратор → Пользователи → Список пользователей

### Рассылка пользователям

#### Всем одобренным пользователям

**Команда:** `/announce_users <текст>`

**Пример:**
```
/announce_users Важное объявление для всех учителей!
```

**Что происходит:**
- Сообщение отправляется в личку каждому одобренному пользователю
- Показывается прогресс-бар
- Статистика: успешно/ошибки/заблокировали бота

#### По предмету

**Команда:** `/announce_subject <предмет> <текст>`

**Пример:**
```
/announce_subject Математика Собрание учителей математики завтра в 15:00
```

**Что происходит:**
- Сообщение получают только пользователи с указанным предметом
- Регистр не важен (Математика = математика)

**Посмотреть доступные предметы:**
- Администратор → Пользователи → Рассылка по предмету

### Управление через меню

**Путь:** Администратор → Пользователи

**Доступные действия:**
- 📋 Список пользователей - показать всех зарегистрированных
- ⏳ Ожидают одобрения - показать заявки на рассмотрении
- 📤 Рассылка всем - инструкция по `/announce_users`
- 📚 Рассылка по предмету - список предметов и инструкция

---

## 🔍 Полезные команды

| Команда | Описание |
|---------|----------|
| `/start` | Главное меню бота |
| `/menu` | Открыть интерактивное меню |
| `/groups` | Список всех групп |
| `/myid` | Узнать свой Telegram ID |
| `/groupid` | Узнать ID группы (выполнить в группе) |
| `/cancel` | Отменить текущее действие |

---

## ⚠️ Частые ошибки и решения

### "Ни одна из указанных групп не найдена"

**Причина:** Неправильный ID группы или группа не добавлена в бот.

**Решение:**
1. Проверьте ID через `/groups`
2. Убедитесь, что ID начинается с `-100`
3. Добавьте группу через `/addgroup` если нужно

### "Message thread not found"

**Причина:** Тема форума была удалена, но бот пытается отправить в неё.

**Решение:**
```
/settopic reset
```

### Бот не отправляет сообщения в группу

**Причина:** У бота нет прав в группе.

**Решение:**
1. Зайдите в группу
2. Настройки → Администраторы
3. Дайте боту права на отправку сообщений

---

## 💡 Лучшие практики

### 1. Организация тегов

Создайте систему тегов:
- `важное` - критичные объявления
- `новости` - новости компании
- `события` - мероприятия
- `hr` - HR-объявления

### 2. Использование шаблонов

Сохраните часто используемые форматы:
- Еженедельные отчёты
- Поздравления
- Напоминания

### 3. Форумы

Для больших организаций:
- Создайте форум с темами по отделам
- Настройте рассылку в нужную тему
- Используйте теги для дополнительной фильтрации

---

## 🎓 Примеры использования

### Пример 1: Срочное объявление для всех

```
/announce ⚠️ ВАЖНО! Завтра офис закрыт на санитарный день.
```

### Пример 2: Объявление для HR-отдела

```
/announce_to hr Напоминание: сдать табели до 20 числа
```

### Пример 3: Рассылка с фото для конкретных групп

```
/announce_groups -1001838199188 [прикрепить фото меню]
```

### Пример 4: Настройка форума

```
1. Зайти в форум "Компания"
2. Открыть тему "📢 Объявления"
3. URL: t.me/c/1838199188/5
4. /settopic 5
5. /announce Теперь все объявления идут в эту тему!
```

---

## 📞 Поддержка

Если возникли проблемы:
1. Проверьте права бота в группах
2. Убедитесь, что ID групп правильные
3. Проверьте логи бота (если есть доступ к серверу)

---

**Версия документации:** 1.0  
**Дата обновления:** 15.12.2025
